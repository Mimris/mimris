# Tasks: Workbench Server Projects

- [x] Add a server-backed workbench project API under `src/pages/api/workbench/`.
- [x] Add a `data/workbench/` runtime storage location.
- [x] Extend the workspace adapter with save-time canonical snapshot generation.
- [x] Preserve original workspace snapshot fields on save through adapter metadata.
- [x] Add `/model?project=<id>` loading support.
- [x] Add minimal project-menu actions for `Save to server` and `Open server project`.
- [x] Verify the feature with `npm run build`.
