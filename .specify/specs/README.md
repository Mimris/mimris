# Specs

Create one folder per active feature or change under this directory.

Example:

```text
.specify/specs/001-idef0-icom-layout/
  spec.md
  plan.md
  tasks.md
```

Use this directory for active work only. Keep stable project reference material in `docs/`.
